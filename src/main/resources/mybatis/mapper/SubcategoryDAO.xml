<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.drawing.springboot.dao.ISubcategoryDAO">

    <!-- 특정 카테고리의 하위 카테고리 조회 -->
    <select id="getSubcategoriesByCategoryId" parameterType="long" resultType="com.drawing.springboot.dto.SubcategoryDTO">
        SELECT subcategory_id AS subcategoryId,
               category_id AS categoryId,
               name,
               url
        FROM subcategories
        WHERE category_id = #{categoryId}
        ORDER BY subcategory_id
    </select>

    <!-- 특정 서브카테고리 조회 -->
    <select id="getSubcategoryById" parameterType="long" resultType="com.drawing.springboot.dto.SubcategoryDTO">
        SELECT subcategory_id AS subcategoryId,
               category_id AS categoryId,
               name,
               url
        FROM subcategories
        WHERE subcategory_id = #{subcategoryId}
    </select>
    
    <select id="getAllSubcategories" resultType="com.drawing.springboot.dto.SubcategoryDTO">
    	SELECT subcategory_id AS subcategoryId,
           	category_id AS categoryId,
           	name,
           	url
    	FROM subcategories
	</select>

    

</mapper>
