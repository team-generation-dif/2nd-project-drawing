<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.drawing.springboot.dao.IProductsDAO">

    <!-- 특정 카테고리 상품 조회 -->
    <select id="getProductsByCategoryId" resultType="com.drawing.springboot.dto.ProductsDTO">
        SELECT p.p_code,
               p.subcategory_id AS subcategoryId,
               p.p_name,
               p.p_color,
               p.p_width,
               p.p_price,
               p.p_image,
               p.p_rating
        FROM products p
        JOIN subcategories s ON p.subcategory_id = s.subcategory_id
        WHERE s.category_id = #{categoryId}
        ORDER BY p.p_code
    </select>

    <!-- 특정 서브카테고리 상품 조회 -->
    <select id="getProductsBySubcategoryId" parameterType="long" resultType="com.drawing.springboot.dto.ProductsDTO">
        SELECT p_code,
               subcategory_id AS subcategoryId,
               p_name,
               p_color,
               p_width,
               p_price,
               p_image,
               p_rating
        FROM products
        WHERE subcategory_id = #{subcategoryId}
        ORDER BY p_code
    </select>

    <!-- 특정 상품 조회 -->
    <select id="getProductById" parameterType="long" resultType="com.drawing.springboot.dto.ProductsDTO">
        SELECT p_code,
               subcategory_id AS subcategoryId,
               p_name,
               p_color,
               p_width,
               p_price,
               p_image,
               p_rating      
        FROM products
        WHERE p_code = #{productId}
    </select>

<!--  	<select id="favoritesByMCode" resultType="com.drawing.springboot.dto.ProductsDTO">
        SELECT p.p_code,
               subcategory_id,
               p_name,
               p_price,
               p_image,
               p_rating,
               review_count
        FROM products p join favorites f on p.p_code = f.p_code
        WHERE f.m_code = 
    </select> -->
	
    <!-- 상품 등록 -->
    <insert id="insertProduct" parameterType="com.drawing.springboot.dto.ProductsDTO">
    	INSERT INTO products (
        	p_code, p_name, p_color, p_width, p_price, p_image, p_rating, subcategory_id
    	)
    	VALUES (
        	#{p_code},
        	#{p_name},
       	 	#{p_color},
       	 	#{p_width},
        	#{p_price},
        	#{p_image},
        	<choose>
            	<when test="p_rating != null">
                	#{p_rating, jdbcType=NUMERIC}
            	</when>
            	<otherwise>
                	NULL
            	</otherwise>
        	</choose>,
        	#{subcategoryId}
    	)
	</insert>

	
	<!-- 전체 상품 조회 -->
	<select id="getAllProducts" resultType="com.drawing.springboot.dto.ProductsDTO">
    	SELECT p_code, p_name, p_color, p_width, p_price, p_image, p_rating, subcategory_id
    	FROM products
    	ORDER BY p_code
	</select>
		
</mapper>
